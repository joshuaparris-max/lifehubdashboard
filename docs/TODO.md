High-priority TODOs

1. Apply XSS mitigations to `dashboard.js` (escapeHtml + safeInsertList) — High
2. Audit `scripts/*.py` for subprocess usage and add server-side validation — High
3. Add unit tests for ICS parsing and filename heuristics — Medium
4. Scaffold package.json + ESLint + Jest and add a GitHub Actions workflow — Medium
5. Refactor `dashboard.js` into modules and add small UI unit tests — Medium
6. Add accessibility fixes (aria, focus trap, keyboard nav) — Medium
7. Implement storage schema versioning for localStorage — Low
8. Add diagnostics export and debugging guidance — Low

If you'd like, I can work on items in order. Reply with which item to start on (e.g., "Start with 1: apply XSS mitigations").
